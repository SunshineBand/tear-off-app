<div class="category-body">
  <div class="category-content">
    <%= render 'flyers/all_flyers_grid', flyers: @category_flyers, heading: @category.name %>

    <%= render 'layouts/projects_list', projects: @projects %>
  </div>
</div>