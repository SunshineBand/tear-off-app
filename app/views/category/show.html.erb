<%= stylesheet_link_tag 'flyers' %>

<%= render 'flyers/flyers_view', flyers: @category.flyers, heading: @category.name, more_link: '/flyers/all' %>

<%= render 'layouts/projects_list', projects: @projects %>